package com.halovoid.lncrawler.ui.core.theme

import androidx.compose.ui.graphics.Color

/**
 * Modern Dark UI color palette for LNCrawler.
 */
/**
 * LNCrawler Neutral Design System
 */
val DarkBackground = Color(0xFF0F0F0F) // Near-black charcoal
val DarkSurface = Color(0xFF1A1A1A)    // Slightly lighter for cards/elevated areas
val DarkSurfaceVariant = Color(0xFF242424) // Distinctive surface for secondary grouping

val PrimaryAccent = Color(0xFF94A3B8) // Warmer neutral gray as default
val BrandAccent = Color(0xFF7C3AED)   // Vibrant Electric Indigo for primary life

val SuccessGreen = Color(0xFF22C55E) // Semantic Success
val WarningAmber = Color(0xFFF59E0B) // Semantic Warning
val ErrorRed = Color(0xFFEF4444)     // Semantic Error
val SupportRose = Color(0xFFF43F5E)   // Warm Rose for support/emotive actions

val DiscordBlurple = Color(0xFF5865F2)
val GitHubOrange = Color(0xFFF34F29)

val PrimaryText = Color(0xFFF8F9FA)   // Off-white
val SecondaryText = Color(0xFF9CA3AF) // Muted gray
val BorderColor = Color(0xFF262626)   // Subtle dividers

// Legacy colors kept for reference or specific use cases
val NearBlack = Color(0xFF121212)
val PureWhite = Color(0xFFFFFFFF)
val LightGray = Color(0xFFE0E0E0)
